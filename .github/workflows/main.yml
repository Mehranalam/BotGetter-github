name: Run Telegram Bot

on:
  schedule:
    - cron: '0 6 * * *'
  push:
    branches:
      - main

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      - name: Install dependencies
        run: npm install

      - name: Run bot script
        run: node bot.js
        env:
          BOT_TOKEN: ${{ 7212552198:AAE10QYLm_qC0e2MV7pV0WYYVvJ66kfKPvI }}
          CHANNEL_ID: ${{ github_get }}
          PROXY_URL: ${{  }}
